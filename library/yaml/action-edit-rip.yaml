lbcli:  
-  action: edit-rip
   vip: "{{ vip }}"
   rip: "{{ rip }}"
   ip: "{{ ip }}"
   port: "{{ port }}"
   weight: "{{ weight }}"
   minconns: "{{ minconns }}"
   maxconns: "{{ maxconns }}"
   encrypted: "{{ encrypted }}"
