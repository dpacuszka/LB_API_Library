lbcli:  
-  action: acl
   function: add
   vip: "{{ vip }}"
   pathtype: "{{ pathtype }}"
   path: "{{ path }}"
   redirecttype: "{{ redirecttype }}"
   location: "{{ location }}"
   bool: "{{ bool }}"
