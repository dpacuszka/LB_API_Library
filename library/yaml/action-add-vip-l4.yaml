lbcli:  
-  action: add-vip
   layer: "{{ layer }}"
   vip: "{{ vip }}"
   ip: "{{ ip }}"
   ports: "{{ ports }}"
   forwarding: "{{ forwarding }}"
   protocol: "{{ protocol }}"
   slave_ip: "{{ slave_ip }}"
