lbcli:  
-  action: edit-vip
   vip: "{{ vip }}"
   ip: "{{ ip }}"
   ports: "{{ ports }}"
   mode: "{{ mode }}"
   persistence: "{{ persistence }}"
   cookiename: "{{ cookiename }}"
   fallback_ip: "{{ fallback_ip }}"
   fallback_port: "{{ fallback_port }}"
   persist_time: "{{ persist_time }}"
   persist_table_size: "{{ persist_table_size }}"
   maxconn: "{{ maxconn }}"
   scheduler: "{{ scheduler }}"
   check_port: "{{ check_port }}"
   check_request: "{{ check_request }}"
   check_receive: "{{ check_receive }}"
   check_host: "{{ check_host }}"
   check_username: "{{ check_username }}"
   appsession_cookie: "{{ appsession_cookie }}"
   forward_for: "{{ forward_for }}"
   http_pipeline: "{{ http_pipeline }}"
   http_pretend_keepalive: "{{ http_pretend_keepalive }}"
   stunneltproxy: "{{ stunneltproxy }}"
   feedback_method: "{{ feedback_method }}"
   fallback_persist: "{{ fallback_persist }}"
   feedback_port: "{{ feedback_port }}"
   check_type: "{{ check_type }}"
   external_check_script: "{{ external_check_script }}"
   tcp_keep_alive: "{{ tcp_keep_alive }}"
   force_to_https: "{{ force_to_https }}"
   timeout: "{{ timeout }}"
   timeout_client: "{{ timeout_client }}"
   timeout_server: "{{ timeout_server }}"
   redirect_code: "{{ redirect_code }}"
   no_write: "{{ no_write }}"
   waf_label: "{{ waf_label }}"
   clear_stick_drain: "{{ clear_stick_drain }}"
   compression: "{{ compression }}"
   autoscale_group: "{{ autoscale_group }}"
   cookie_maxidle: "{{ cookie_maxidle }}"
   cookie_maxlife: "{{ cookie_maxlife }}"
   source_address: "{{ source_address }}"
   backend_encryption: "{{ backend_encryption }}"
   enable_hsts: "{{ enable_hsts }}"
   hsts_month: "{{ hsts_month }}"
   xff_ip_pos: "{{ xff_ip_pos }}"
   invalid_http: "{{ invalid_http }}"
   send_proxy: "{{ send_proxy }}"
   as_port: "{{ as_port }}"
   http_request: "{{ http_request }}"
   stunnel_source: "{{ stunnel_source }}"
   proxy_bind: "{{ proxy_bind }}"
   slave_ip: "{{ slave_ip }}"
   tunneltimeout: "{{ tunneltimeout }}"
   redispatch: "{{ redispatch }}"
   fallback_encrypt: "{{ fallback_encrypt }}"
   http_reuse_connection: "{{ http_reuse_connection }}"
   tproxy: "{{ tproxy }}"
