lbcli:  
-  action: termination
   type: "{{ type }}"
   function: csr
   csrname: "{{ csrname }}"
   city: "{{ city }}"
   province: "{{ province }}"
   country: "{{ country }}"
   organisation: "{{ organisation }}"
   unit: "{{ unit }}"
   domain: "{{ domain }}"
   email: "{{ email }}"
   csrsize: "{{ csrsize }}"
   signalgorithm: "{{ signalgorithm }}"
   days: "{{ days }}"
