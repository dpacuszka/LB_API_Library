lbcli:  
-  action: headers
   function: add
   vip: "{{ vip }}"
   header_type: "{{ header_type }}"
   header_option: "{{ header_option }}"
   header_name: "{{ header_name }}"
   header_value: "{{ header_value }}"
