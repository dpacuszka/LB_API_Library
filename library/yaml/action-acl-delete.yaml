lbcli:  
-  action: acl
   function: delete
   vip: "{{ vip }}"
   pathtype: "{{ pathtype }}"
   path: "{{ path }}"
   redirecttype: "{{ redirecttype }}"
   location: "{{ location }}"
   bool: "{{ bool }}"
