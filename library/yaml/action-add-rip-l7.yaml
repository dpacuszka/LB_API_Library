lbcli:  
-  action: add-rip
   vip: "{{ vip }}"
   rip: "{{ rip }}"
   ip: "{{ ip }}"
   weight: "{{ weight }}"
   port: "{{ port }}"
   minconns: "{{ minconns }}"
   maxconns: "{{ maxconns }}"
   encrypted: "{{ encrypted }}"
